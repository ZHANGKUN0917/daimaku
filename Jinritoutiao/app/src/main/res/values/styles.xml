<resources>

    <!-- Base application theme. -->
    <!--白天模式-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="ground">@android:color/white</item>
    </style>
   <style name="TabLayoutTextStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
       <item name="android:textSize">20sp</item>
   </style>
    <!--夜间模式-->
    <style name="NightAppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/nightcolorPrimary</item>
        <item name="colorPrimaryDark">@color/nightcolorPrimaryDark</item>
        <item name="colorAccent">@color/nightcolorAccent</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="ground">@android:color/black</item>
    </style>
</resources>
